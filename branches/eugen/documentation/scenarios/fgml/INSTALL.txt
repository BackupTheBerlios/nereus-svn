Anweisungen zum installieren des fgml

1. kopieren des servermoduls in den server
    cp -r servermodule/ ../../server/scenario/fgml/

2. kopieren der agents in den client
    cp -r agents/ <Pfad zum Client>/client/scenario/fgml/
